------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------- BASE DE DONNEES MYSQL---------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

-- Pour récupérer les informations de l’étudiant le plus agé, on peut utiliser la requête SQL suivante :

            SELECT * FROM etudiants WHERE age = (SELECT MAX(age) FROM etudiants);

-- Affiche :

mysql> SELECT * FROM etudiants WHERE age = (SELECT MAX(age) FROM etudiants);
+----+-------+--------+-----+-----------------------------+---------+
| id | nom   | prenom | age | email                       | famille |
+----+-------+--------+-----+-----------------------------+---------+
|  2 | Steak | Chuck  |  45 | chuck.steak@laplateforme.io | Steak   |
+----+-------+--------+-----+-----------------------------+---------+
1 row in set (0,00 sec)